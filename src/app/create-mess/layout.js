export const metadata = {
  title: "Create Mess",
  description: "Set up and register a new mess on EasyMess.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CreateMessLayout({ children }) {
  return children;
}
