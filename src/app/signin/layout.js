export const metadata = {
  title: "Sign In",
  description: "Log in to your EasyMess account to manage meals, expenses, and monthly bills.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SigninLayout({ children }) {
  return children;
}
