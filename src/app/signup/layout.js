export const metadata = {
  title: "Sign Up",
  description: "Create an EasyMess account to manage your bachelor mess or hostel meals.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignupLayout({ children }) {
  return children;
}
