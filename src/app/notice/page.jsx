  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);@  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);@  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);@  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);X  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);@  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);@  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);J  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);X  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);U  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);U  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);J  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);U  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);J  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);H  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ং  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ণ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);J  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);থ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);H  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);U  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);J  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);H  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);|  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);গ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);জ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);R  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);দ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);খ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);গ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);গ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);!  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ও  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);দ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);গ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);গ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);দ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ৈ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);H  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);উ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ও  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);দ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);দ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ূ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);B  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);Q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ও  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);F  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ধ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);থ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);থ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);_  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);,  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);X  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);খ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ণ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ণ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);খ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);W  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);�  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);X  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);L  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ণ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);[  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);]  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);q  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);$  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);`  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);P  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);W  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);I  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);E  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);O  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ং  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ষ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ণ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);হ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);S  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ো  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ও  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);অ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);শ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);M  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);*  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);&  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);j  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);z  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);9  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);4  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);প  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ু  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ছ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);চ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);এ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ই  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ক  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);জ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ফ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);র  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ে  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);আ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);স  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ম  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);্  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ভ  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ন  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);য়  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);।  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);A  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);.  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);7  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);8  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);3  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ব  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);া  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ত  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);k  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);C  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);T  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);(  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);N  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);1  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);y  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);2  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);f  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);m  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);6  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);5  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);0  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);x  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);w  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);h  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);c  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);s  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);-  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);p  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);r  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);{  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);a  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);g  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);=  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);?  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ড  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ল  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ি  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);ট  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);:  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);D  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);l  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);e  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);"  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);b  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);u  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);t  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);o  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);n  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);<  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);/  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);d  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);i  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);v  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);>  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);   const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);)  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);;  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);}  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);
  const [role, setRole] = useState("");

  useEffect(() => {
    if (typeof window !== "undefined" && userId) {
      const cachedRole = sessionStorage.getItem(`user_role_${userId}`);
      if (cachedRole) setRole(cachedRole);
    }
  }, [userId]);