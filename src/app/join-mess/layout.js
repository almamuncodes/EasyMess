export const metadata = {
  title: "Join Mess",
  description: "Join an existing mess on EasyMess using an invite code.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function JoinMessLayout({ children }) {
  return children;
}
